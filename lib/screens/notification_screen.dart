import 'package:flutter/material.dart';

class NotificationgScreen extends StatelessWidget {
  const NotificationgScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Notification'),
    );
  }
}
